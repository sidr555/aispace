extends main_layout

block page
  .page(data-page="index")

          .navbar
            .navbar-inner
              .center AISpace

          .toolbar
            .toolbar-inner
              //if user
              //  a.link(href="/auth/logout") Выход
              //else
              a.link(href="/db") DB
              //a.link(href="/user/register") Регистрация
              a.link.link-login(href="#") Вход

          .page-content
            p Вводная информация о сервисе для пользователей и владельцев витрин

